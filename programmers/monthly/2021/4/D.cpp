// u -> v
// 3000개. 1번에 가는 최대값, 2번에 가는 최대값.
// 2턴에 얻을 수 있는 금액 최대 100원.
// m[u] = 1턴에 얻을 수 있는 최대값.
//????????

#include <string>
#include <vector>

using namespace std;

vector<long long> solution(int n, int z, vector<vector<int>> roads, vector<long long> queries) {
    vector<long long> answer;
    return answer;
}